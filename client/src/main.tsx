import React from 'react'
import ReactDOM from 'react-dom/client'
import DangenCore from './DangenCore.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DangenCore />
  </React.StrictMode>,
)
